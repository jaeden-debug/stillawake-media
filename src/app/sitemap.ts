import {MetadataRoute} from "next";import {posts,siteUrl} from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const pages=["","about","portfolio","stillawake-times","contact"].map(p=>({url:`${siteUrl}/${p}`,lastModified:new Date()}));const blog=posts.map(p=>({url:`${siteUrl}/stillawake-times/${p.slug}`,lastModified:new Date(p.date)}));return[...pages,...blog]}
